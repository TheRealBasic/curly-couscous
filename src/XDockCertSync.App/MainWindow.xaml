<Window x:Class="XDockCertSync.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="XDock Certificate Sync Settings"
        Height="420"
        Width="620"
        ResizeMode="CanMinimize">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="180" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Margin="0,0,12,12" VerticalAlignment="Center" Text="IP / Hostname" />
        <TextBox Grid.Row="0" Grid.Column="1" Margin="0,0,0,12" Text="{Binding Hostname, UpdateSourceTrigger=PropertyChanged}" />

        <TextBlock Grid.Row="1" Grid.Column="0" Margin="0,0,12,12" VerticalAlignment="Center" Text="Username" />
        <TextBox Grid.Row="1" Grid.Column="1" Margin="0,0,0,12" Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" />

        <TextBlock Grid.Row="2" Grid.Column="0" Margin="0,0,12,12" VerticalAlignment="Center" Text="Password" />
        <PasswordBox Grid.Row="2" Grid.Column="1" Margin="0,0,0,12" PasswordChanged="PasswordBox_OnPasswordChanged" />

        <TextBlock Grid.Row="3" Grid.Column="0" Margin="0,0,12,12" VerticalAlignment="Center" Text="Polling Interval (sec)" />
        <TextBox Grid.Row="3" Grid.Column="1" Margin="0,0,0,12" Text="{Binding PollingIntervalSeconds, UpdateSourceTrigger=PropertyChanged}" />

        <TextBlock Grid.Row="4" Grid.Column="0" Margin="0,0,12,12" VerticalAlignment="Center" Text="Output Directory" />
        <TextBox Grid.Row="4" Grid.Column="1" Margin="0,0,0,12" Text="{Binding OutputDirectory, UpdateSourceTrigger=PropertyChanged}" />

        <Button Grid.Row="5" Grid.Column="1" HorizontalAlignment="Right" Padding="18,8" Content="Save Settings" Command="{Binding SaveCommand}" />

        <TextBlock Grid.Row="6" Grid.Column="0" Grid.ColumnSpan="2" Margin="0,16,0,0" Foreground="DarkSlateGray" Text="{Binding StatusMessage}" TextWrapping="Wrap" />
    </Grid>
</Window>
